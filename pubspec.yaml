name: whatsapp_route_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # الأيقونات والتصميم
  cupertino_icons: ^1.0.8

  # الحزم الأساسية والأمان
  flutter_secure_storage: ^9.2.4
  local_auth: ^2.3.0

  # الحزم الخاصة بالميزات المضافة في التطبيق
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.13.0
  url_launcher: ^6.3.0
  pdf: ^3.10.8
  printing: ^5.13.0
  sqflite: ^2.3.3+1
  path: ^1.9.0
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true